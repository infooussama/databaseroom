<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.relationbdd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RelationBdd">
        <activity android:name=".Mylocation"></activity>
        <activity android:name=".ShowResultInmaps" />
        <activity android:name=".DetailLigne" />
        <activity android:name=".DetailTeleStation" />
        <activity android:name=".CalculeItineraire" />
        <activity android:name=".ListStation" />
        <activity android:name=".DetailBusStation" />
        <activity android:name=".DetailTramStation" />
        <activity android:name=".DetailMetroStation" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>